// Ambient declarations for the dashboard build.
declare global {
  /** The root VERSION file's contents, e.g. `0.1.0`; web/vite.config.ts injects it. */
  const __APP_VERSION__: string;
}
export {};
